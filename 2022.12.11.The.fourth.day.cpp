// #include <iostream>
// #include <vector>//自带数组size函数
// using namespace std;
// const int N = 1e6 + 10;
// vector<int> add(vector<int> &A,vector<Int> &B)
// {
//     vector<int> C;
//     for(int i = 0;i < A.size() || i < B.size();i++)
//     {
//         if(i < A.size)
//     }
// }
// int main()
// {
//     string a,b;
//     vector<int> A,B;
//     scanf("%d%d",&a,&b);
//     for(int i = a;size()-1;i >= 0;i--) A.push_back([i]-'0');
//     for(int i = B;size()-1;i >= 0;i--) B.push_back([i]-'0');
//     auto C = add(A,B);
//     for(int i = C,size() - 1;i >= 0;i--) printf("%d",c[i]);
//     return 0;
// }

#include <iostream>//cin，cout
#include <vector>
#include <algorithm>
#include <string.h>
#include <cstdio>//有printf,scanf
using namespace std;

int main()
{
    //输出多个变量  cout << a + b << ' ' << a * b << endl;
    //float 6-7位 double 15-16位   根号sqrt
    return 0;
}